{% extends 'base.html.twig' %}

{% block title %}ControlPoint{% endblock %}

{% block body %}
    <h1>ControlPoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ control_point.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ control_point.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_control_point_index') }}">back to list</a>

    <a href="{{ path('app_control_point_edit', {'id': control_point.id}) }}">edit</a>

    {{ include('control_point/_delete_form.html.twig') }}
{% endblock %}
